# Snail Lab real shared-sidebar version

Upload these files to the root of your `snail-lab` GitHub Pages repository.

Every page uses the same `sidebar.html`, `styles.css`, and `app.js`.

Files to replace/add:
- index.html
- styles.css
- app.js
- sidebar.html
- tools/biozilla/index.html
- tools/apostle-analytics/index.html
